services:
  db:
    image: postgres:16-alpine
    environment: { POSTGRES_USER: shrt, POSTGRES_PASSWORD: shrt, POSTGRES_DB: shrt }
    ports: ["5432:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]
volumes: { pgdata: {} }